---
repos:
  - repo: local
    hooks:
      - id: go-mod-tidy
        name: go mod tidy
        entry: go mod tidy
        language: system
        files: '^go\.(mod|sum)$|\.go$'
        pass_filenames: false
        description: "Ensures go.mod and go.sum are up to date"
